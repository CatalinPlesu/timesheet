# API base URL — leave empty to use same-origin (nginx proxies /api/* to API container)
VITE_API_BASE_URL=

# Example for local dev where API runs on different port:
# VITE_API_BASE_URL=http://localhost:5004
